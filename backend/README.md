# Backend - Plataforma RPA (esqueleto)

Estrutura inicial do backend Flask com SQLAlchemy.

Para rodar com Docker Compose:

```bash
docker compose up --build
```

Ajuste variáveis em `.env` ou `docker-compose.yml` conforme necessário.
